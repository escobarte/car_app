Задача A — пересчёт одометра и статусов (ТЗ 5.1, 6.3):

Исправь два бага. (1) При создании регламента type=mileage подставляй last_odometer = current_odometer, для type=time — last_date = сегодня, чтобы новый регламент стартовал «в норме» с полным интервалом (ТЗ 6.3). (2) Заведи единый пересчёт CAR.current_odometer = максимум из базового пробега машины и всех odometer в FUEL_ENTRY/EXPENSE/SERVICE_RECORD. Вызывай его после каждого добавления/редактирования/удаления записи. После пересчёта одометра пере-оценивай статусы всех регламентов (ТЗ 6.3). Проверка: добавил заправку +76 → удалил → одометр вернулся, статус регламента вернулся.

Задача B — валидация и откат подтверждения (ТЗ 6.3):

В форме подтверждения регламента («Сделано») сделай поля пробег и дата обязательными — не давай сохранить пустые. При удалении записи SERVICE_RECORD, которая закрывала регламент (reminder_id заполнен), откатывай last_odometer/last_date регламента на предыдущее значение и пересчитывай статус (ТЗ 6.3).

Задача C — косметика:

На кнопке «Добавить регламент» убери дублирующий «+» в тексте, оставь только иконку-плюс. Цвета через тему, текст через t().

Задача D — аудит уведомлений (перед починкой):

Покажи, как сейчас устроены push-уведомления (ТЗ 6.3, 7.2). Ответь по коду, без изменений: (1) где и когда планируются/отправляются уведомления; (2) для type=time — на какое время суток ставится локальное уведомление (есть ли хардкод вроде 9:00); (3) для type=mileage — есть ли вообще триггер уведомления при пересчёте статуса после изменения одометра, или уведомления шлются только для time. Дай короткий отчёт — потом починим по результату.

Задача E — расход по умолчанию + подсказка (ТЗ 6.2):

В форме заправки поставь тумблер «Полный бак» включённым по умолчанию (большинство заправок полные). Если расход не может быть посчитан (нет предыдущей полной заправки), в карточке «Средний расход» под «—» показывай мелкую подсказку-причину: при первой полной — «нужна ещё одна полная заправка», при неполных — «отметьте полный бак для расчёта». Текст через t(), цвета через тему.

Задача E (финальная версия):

В форме добавления заправки (ТЗ 4.3, 5.2, 6.2):

Тумблер «Полный бак» по умолчанию включён.
В карточке «Средний расход» на дашборде, когда расход посчитать нельзя, под «—» показывай мелкую строку-подсказку с причиной:
нет ни одной полной заправки → «отметьте полный бак для расчёта»;
есть ровно одна полная заправка → «нужна ещё одна полная заправка»;
есть неполные, но нет второй полной → та же подсказка про полный бак.
Не трогай саму формулу расчёта (ТЗ 6.2, full-to-full) — меняется только дефолт тумблера и подсказка.
Все тексты через t() (RU + EN), цвета через тему. Хардкод запрещён.
Подсказку(и) добавь новыми ключами в оба словаря.