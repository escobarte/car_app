3. Кнопки на экранах 1 и 2 на разной высоте — разный нижний отступ футера, надо привести к одному.

Промпт для КК (#2 и #3 вместе — это футер):

Onboarding: футер и клавиатура (пересмотр прошлого поведения).

1. ОТМЕНИТЬ подъём кнопки над клавиатурой на экране 2. Кнопка Next больше
   НЕ должна вылезать поверх полей. Убрать KeyboardAvoidingView-подъём
   футера. Теперь: при открытой клавиатуре футер (точки + кнопка) остаётся
   внизу и просто уходит ПОД клавиатуру. Поля ввода (Name, Odometer) вверху
   и доступны. Клавиатура закрывается по Done или тапу вне поля.

2. Единая высота футера на ВСЕХ экранах онбординга (1, 2, 3). Сейчас кнопка
   Get Started (экран 1) и Next (экран 2) на разной высоте — разный нижний
   отступ. Вынести футер в общий компонент с одинаковым paddingBottom
   (учесть safe-area снизу через useSafeAreaInsets), чтобы кнопка стояла
   на одном уровне на всех трёх экранах.

Цвета через тему, текст через t(). Git не трогай. docs/CHANGELOG_work.md.

#1  — не splash чинить, а перенести запрос. Замени предыдущий промпт #1 на этот:

Онбординг, разрешение на уведомления: сейчас запрашивается сразу при старте
приложения — это неверно (низкий процент согласий + под диалогом просвечивает
светлый splash).

1. Убрать запрос permission на уведомления со старта приложения.
2. Запрашивать его в КОНЦЕ онбординга: при нажатии кнопки «Поехали» на
   экране 3 «Всё готово», перед переходом на дашборд. Последовательность:
   тап «Поехали» → requestPermissionsAsync() → затем навигация на дашборд
   (переход выполнять независимо от того, разрешил юзер или нет).
3. Если пользователь уже проходил онбординг раньше (не первый запуск) —
   повторно разрешение не спрашивать.
4. Проверить, что при показе системного диалога под ним тёмный фон экрана 3,
   а не светлый splash.

Цвета через тему, текст через t(). Git не трогай. docs/CHANGELOG_work.md.


--
Update #4
Убрать мелькание белого фона при запуске приложения. Сделать старт
бесшовным, без картинки-заставки.

1. Нативный splash и фон окна сделать одноцветными тёмными, совпадающими
   с фоном приложения (theme background, #000/#0a0a0f). Убрать картинку
   (иконку машины) со splash — оставить просто тёмный фон, без изображения.
2. В app.json / app.config: splash.backgroundColor = тёмный; убрать/очистить
   splash.image (или заменить на пустую/прозрачную). Проверить оба режима —
   light и dark splash-конфиг, если заданы раздельно, оба тёмные.
3. Android: задать windowBackground / android:background стартового окна в
   тёмный цвет, чтобы первый кадр окна был тёмным, а не белым (сейчас белый).
   При необходимости настроить через expo-splash-screen плагин в app.config.
4. Цель: при нажатии Open виден только ровный тёмный фон, плавно переходящий
   в онбординг. Никакого белого кадра и никакой иконки-заставки.

Git не трогай. docs/CHANGELOG_work.md.

