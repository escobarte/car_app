Свайп возможен — expo-router поддерживает Material Top Tabs, но с приподнятой «+» есть нюанс, отмечу в промпте.

**Промпт 1 — мелочь на дашборде**
```
Дашборд, карточка «Расходы за месяц»: убрать стрелку/индикатор под суммой —
она там лишняя. Тап по карточке остаётся рабочим, просто без визуального
указателя.
Цвета через объект темы, текст через t(). Git не трогай.
Запиши в docs/CHANGELOG_work.md.
```

**Промпт 2 — Статистика**
```
Экран Статистика, вкладка «Расходы».

1) Блок «По категориям»: строки категорий сделать нажимаемыми, как уже
   сделаны столбики графика. Тап открывает модалку с деталями категории:
   сумма за период, число записей, доля в процентах, список записей
   (дата, описание, сумма). Стиль модалки — тот же компонент, что на дашборде.

2) Добавить на вкладку «Расходы» блок метрик-карточек, как уже сделано
   на вкладке «Расход (л/100)»: сетка 2х2 под графиком.
   Метрики: средний расход в месяц, всего за период,
   минимальный месяц, максимальный месяц.
   Оформление один в один с существующими карточками л/100.

Цвета через объект темы, текст через t() (RU+EN), хардкод запрещён.
Git не трогай. Запиши в docs/CHANGELOG_work.md.
```

**Промпт 3 — свайп между вкладками**
```
Навигация: добавить переключение вкладок свайпом влево/вправо
между Главная — История — Сервис — Статистика.

Реализация: перевести нижнюю навигацию на Material Top Tabs
(@react-navigation/material-top-tabs) с tabBarPosition="bottom",
либо обернуть экраны в react-native-pager-view — выбери, что чище
ляжет на текущую структуру expo-router.

ВАЖНО, не сломать:
- Кнопка «+» приподнята в центре и НЕ является экраном — она открывает
  Action Sheet. При свайпе её пропускаем: порядок свайпа
  Главная → История → Сервис → Статистика, без остановки на «+».
- Внешний вид навбара должен остаться идентичным текущему
  (иконки, подписи, градиент на «+», активный цвет).
- Свайп не должен конфликтовать с горизонтальными жестами внутри экранов
  (свайп по записи в Истории для удаления/редактирования) — проверь это
  отдельно и скажи, если есть конфликт.

Скажи, нужны ли новые пакеты и требуется ли prebuild/пересборка APK.
Цвета через объект темы, текст через t(). Git не трогай.
Запиши в docs/CHANGELOG_work.md.
```

Промпт 3 самый рискованный — давай его последним и отдельно, там перестройка навигации.

**Промпт 4 — подтверждение при создании регламента + время уведомлений**
```
Две связанные правки по регламентам.

1) Время push-уведомлений
Сейчас регламенты типа time считаются по дням, но время срабатывания
не задано явно. Зафиксируй: все уведомления по регламентам приходят
в 9:00 утра по часовому поясу телефона.
Планирование через календарный триггер с hour: 9, minute: 0.
Если момент срабатывания уже прошёл сегодня — планируем на следующий
подходящий день, а не мгновенно.
Применить и к типу mileage (когда статус переходит в «пора»),
и к типу time.

2) Pop-up после создания регламента
После сохранения нового регламента (и после редактирования существующего)
показывать модалку-подтверждение с понятным человеку текстом:
- название регламента;
- как считается: «каждые 8 000 км» или «каждые 365 дней»;
- когда предупредит: «за 500 км до срока» / «за 25 дней до срока»;
- конкретная дата и время срабатывания для типа time —
  например «Уведомление придёт 15 августа 2026 в 9:00»;
- для типа mileage — при каком пробеге сработает:
  «Уведомление придёт при пробеге 213 906 км».
Кнопка «Понятно». Стиль модалки — тот же компонент, что используется
для деталей на дашборде.

Если уведомления выключены в Settings — в модалке дополнительной строкой
предупредить, что push не придёт, пока они отключены.

Цвета через объект темы, текст через t() (RU+EN), хардкод запрещён.
Даты форматировать по текущему языку интерфейса.
Git не трогай. Запиши в docs/CHANGELOG_work.md.
Обнови docs/ТЗ_приложение_авто_финал.md §6.3 — зафиксируй время 9:00.
```
**Промпт 5 — «Название машины + шестерёнка на всех вкладках»**
Две правки по UI.

1) Изменение названия машины
Сейчас название машины (CAR.name) поменять нельзя.
Добавь в Settings, секция машины, первым пунктом «Название машины»
с текущим значением справа. Тап открывает экран или модалку
с полем ввода и кнопкой сохранения.
Валидация: пустое имя не сохраняем, максимум ~40 символов.
Проверь, где название отображается в приложении, и убедись,
что оно обновляется везде сразу после сохранения.

2) Шестерёнка на всех вкладках
Сейчас иконка настроек есть только на Дашборде.
Добавь её в правый верхний угол на экранах История, Обслуживание
и Статистика. Внешний вид, размер, отступ и поведение — один в один
как на Дашборде, открывает тот же экран Settings.
Вынеси в общий компонент, чтобы не дублировать код.
Учти, что на этих экранах есть заголовок — шестерёнка должна встать
на одну линию с ним, не наезжая на контент.

Цвета через объект темы, текст через t() (RU+EN), хардкод запрещён.
Git не трогай. Запиши в docs/CHANGELOG_work.md.
**Промпт 2 - фиксикс**
Правки по экрану Статистика — предыдущая задача выполнена неверно.

1) Карточки метрик были ПЕРЕНЕСЕНЫ с вкладки «Расход (л/100)» на «Расходы».
   Нужно было продублировать, а не перенести.
   Верни блок метрик на вкладку «Расход (л/100)» в прежнем виде
   (средний расход, пробег за период, минимум, максимум)
   и оставь новый блок на вкладке «Расходы» со своими метриками
   (средний расход в месяц, всего за период, минимальный месяц,
   максимальный месяц). Две вкладки — два независимых блока.

2) Карточки метрик сделать нажимаемыми на ОБЕИХ вкладках.
   Тап открывает модалку с пояснением метрики:
   что именно посчитано, за какой период, из скольких записей,
   и по каким записям — списком (дата, значение).
   Например «Минимум 6.4 л/100 км» → показать, к какой заправке
   это относится: дата, пробег, литры.
   Стиль модалки — тот же общий компонент.

3) Проверь, что блок «По категориям» на вкладке «Расходы»
   действительно кликается — это было в прошлой задаче,
   подтверди что работает.

Цвета через объект темы, текст через t() (RU+EN), хардкод запрещён.
Git не трогай. Запиши в docs/CHANGELOG_work.md.
---
**Prompt 6_AutoScaling**
Задача только по документации, код не менять.

Добавь в docs/дизайн_система.md новый раздел «8. Адаптивность»
с правилами вёрстки, чтобы экраны не ломались на разных устройствах.

Содержание:
- Целевой диапазон: от 360dp (Samsung S10, бюджетники) до планшетов.
- Запрещено: фиксированная ширина в px у кнопок, таблеток, карточек.
  Только flex: 1, проценты или flexBasis.
- Высота: minHeight вместо height — иначе ломается при крупном
  системном шрифте Android.
- Ряды кнопок и переключателей: контейнер flexDirection row,
  все элементы flex: 1, равные gap.
- Текст в кнопках и таблетках: numberOfLines={1},
  adjustsFontSizeToFit, minimumFontScale 0.85.
- Сетки метрик 2x2: flexWrap, элементы ~48% ширины, не фиксированные px.
- Длинные значения (суммы, пробег): не обрезать, уменьшать шрифт.
- Проверка каждого нового экрана: обязательно посмотреть на английском —
  EN-строки длиннее русских и первыми ломают вёрстку.
- Иконки и отступы: фиксированные px допустимы, они не зависят от текста.

Оформи в стиле остальных разделов документа. Русский язык.
Git не трогай. Запиши в docs/CHANGELOG_work.md.

**Prompt 6_Audit Auto Scaling**
Разовый аудит адаптивности по правилам docs/дизайн_система.md раздел 8.

Пройди по всем экранам и компонентам приложения и найди места,
нарушающие правила: фиксированные width/height у текстовых элементов,
ряды кнопок без flex: 1, отсутствие numberOfLines у текста в кнопках,
сетки на фиксированных размерах, длинные значения без autoFit.

СНАЧАЛА покажи мне список найденного: файл, строка, что не так,
что предлагаешь. НЕ правь ничего до моего подтверждения.

Разбей на три группы по риску:
1) реально сломается на 360dp;
2) сломается при крупном системном шрифте Android;
3) косметика, можно отложить.

По каждой группе укажи примерное число файлов, которые придётся тронуть.

**Prompt 6 Solving Auto Scaling**
Подтверждаю. Исправь все три группы за один проход.

Дополнительно:
- В пункте 3 (recentAmount) и 4 (metricValue) сначала проверь,
  не сломается ли вёрстка от adjustsFontSizeToFit при коротких значениях —
  шрифт не должен «прыгать» между соседними ячейками.
- Пункт 10 (select-currency symbol) — width на minWidth,
  но убедись, что символы валют остались выровнены в колонку.

После правок дай короткий список: файл — что изменено.
Ничего кроме перечисленного в аудите не трогай.

Git не трогай. Запиши в docs/CHANGELOG_work.md.