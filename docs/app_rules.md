Recap по приложению.
1. Для того чтобы запустить expo server:
	a) powershell [path:  A:\car_app>]
	b) npx expo start -c  //  npx expo start -c --offline

2. В конце Кажлуй задачи пиши: запиши в CHANGELOG_work.md;
2.1 когда протестил — работает, отметь как проверено.

---

Keystore создан — car_release.jks лежит в корне проекта.

Сейчас важно: сохрани в надёжное место (менеджер паролей + облако):

сам файл car_release.jks,
пароль keystore = kalypsopg13@@
alias car_release.