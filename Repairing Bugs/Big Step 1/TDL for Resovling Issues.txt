Понял: «Сервис» = всё, что чинится / меняется / добавляется в авто.
Тогда логика фильтра такая (заложу в промпт, поправь если что):

Сервис = записи обслуживания (SERVICE_RECORD) + расходы категории service.
Расходы = все EXPENSE кроме категории service (чтобы не дублировались в двух фильтрах).
Старые импортированные «Сервис» лежат как EXPENSE.category=service → после правки сами попадут в фильтр «Сервис».

Дальше иду с ранее озвученными дефолтами (#1 только формат даты, #6 — 5 последних, тап открывает модалку деталей из #3, сортировка по дате/сумме).
Ниже — 4 готовых задачи для Claude Code. Давай по одной, после каждой тестируй на телефоне. Задачу C (детали) делай раньше D, т.к. модалку деталей переиспользует #6.

Задача A — Дашборд (шапка + последние транзакции). ТЗ 4.1, дизайн-система.
Правки на экране Дашборд (Home):

1. В шапке вместо "Июль 2026 г." выводить полную текущую дату формата
   "20 июля 2026" (день + месяц прописью в родительном + год), с учётом
   локали через i18n (RU/EN). Строка "Расходы за месяц" и подсчёт суммы
   остаются как есть — период по календарному месяцу не меняем.

2. Ниже блока "Напоминания" добавить новый блок "Последние транзакции":
   - заголовок секции стилем как "Напоминания";
   - 5 последних записей (заправки + расходы + сервис), сортировка по дате
     убыв.;
   - каждая строка: иконка типа, короткое описание, дата, сумма справа;
   - тап по строке открывает модалку деталей записи (тот же компонент, что
     на экране История — если его ещё нет, я дам отдельной задачей, пока
     оставь onPress-заглушку с TODO).

Цвета только через объект темы, весь текст через t(). Git не трогай.

Задача B — Клавиатура перекрывает поля в модалках. ТЗ 4.4, 6.5.
Баг: в Bottom Sheet "Отметить выполнение" (регламенты) и в форме
Сервис → "Сделано" экранная клавиатура перекрывает нижние поля
(Пробег / Стоимость / Заметка) — их не видно при вводе.

Исправь во всех модалках с вводом:
- обернуть содержимое в KeyboardAvoidingView (behavior: iOS 'padding',
  Android 'height') + ScrollView с keyboardShouldPersistTaps="handled";
- при фокусе поле должно проматываться в видимую зону над клавиатурой;
- кнопка "Подтвердить" не должна улетать под клавиатуру.

Цвета через тему, текст через t(). Git не трогай.

Задача C — История: детали записи + фильтр «Сервис» + сортировка. ТЗ 4.2, 5.x, 6.5.
Экран История, три правки:

1. Детали записи: тап по любой строке открывает модалку с полными данными
   записи (для заправки: дата, пробег, литры, сумма, цена/л, полный бак,
   расход; для расхода: дата, категория, описание, пробег, сумма; для
   сервиса: дата, пробег, стоимость, заметка). Сделай переиспользуемый
   компоненть модалки — он понадобится и на Дашборде.

2. Фильтр "Сервис" сейчас пустой. Логика фильтров:
   - "Сервис"  = записи SERVICE_RECORD + расходы EXPENSE с категорией
     service (key='service');
   - "Расходы" = все EXPENSE КРОМЕ категории service;
   - "Заправки" = FUEL_ENTRY; "Всё" = всё.

3. Добавить сортировку списка: переключатель по дате (новые/старые) и по
   сумме (больше/меньше).

Цвета через тему, текст через t(). Git не трогай.

Задача D — Статистика: выделение столбика месяца. ТЗ 4.5, дизайн-система.
Экран Статистика, график по месяцам: при тапе по столбику этот столбик
становится активным (подсветка акцентом/градиентом по дизайн-системе,
остальные приглушены), рядом/над графиком показывается сумма выбранного
месяца. По умолчанию выбран последний месяц.

Цвета только через объект темы. Git не трогай.